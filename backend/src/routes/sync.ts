import express from 'express';

const router = express.Router();

// Placeholder for sync routes
router.get('/', (req, res) => {
  res.json({ message: 'Sync routes coming soon' });
});

export default router; 